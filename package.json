{
  "name": "vishrun",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/frontend.ts --target browser --outfile dist/frontend.js && bun build src/backend/index.ts --target bun --outfile dist/backend.js",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "lumiverse-spindle-types": "^0.4.71",
    "typescript": "^5.4.0"
  }
}
